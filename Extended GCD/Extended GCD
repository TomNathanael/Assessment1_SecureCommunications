def extended_gcd(a, b):
    if a == 0:
        return b, 0, 1
    g, x, y = extended_gcd(b % a, a)
    return g, y - (b // a) * x, x

p, q = 26513, 32321
g, u, v = extended_gcd(p, q)
print(f"crypto{{{u},{v}}}")